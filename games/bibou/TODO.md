# TODO

random list of things. Agents, do not use this list as something you need to implement. It is here as a note for me to not forget things, don't over analyse this.

* add ennemies: will need to decide how they move, what actions they have available, how that is shown to the player, and the play order. For example ennemies can attack after each action, but this needs to show clearly in the UI. it should also not be just a copy of Into the Breach as it would be too complex. Maybe ennemies have different movements and if they reach the character, the game is over? once they exist, they should be able to pick up collectibles same as the character, and act as another indestructible/stuck obstacle a crate can be crushed against, same as the character does now.
* decide what other collectibles do beyond the key (new actions, bonuses, purely symbolic ones, etc) — the `required`-gates-the-goal mechanic is only one use of the collectible system. a collectible that *grants* an action (pick up the flip, then use it) would fit the current "which action, when" challenge better than another gate.
* rotate is gone for now — it never did anything shift couldn't. if it comes back it needs a reason to exist that isn't "one cardinal step".
* the levels teach one idea each and run to 27 now. every mechanic in `core/rules.js` has a level that needs it, and levels 8-27 are new arrangements of those same mechanics — mostly geometry: sealed cells, sealed corridors, mirror tiles, seams. `tests/solver.js` is what keeps them honest (winnable, and actually needing the card they hand out). the space that's left for arrangement alone is thinner now, so the next batch probably does want a new mechanic.
* crates are all drawn identically, including the one holding the key. fine while levels have at most one crate that matters, but a level with several would need the carrier marked — probably the collectible's glyph drawn small on the crate.
* dead ends are currently only recoverable via the retry button. if that turns out to feel bad, undo is the alternative — but undo makes "spend the right action" much softer, so try retry first.
